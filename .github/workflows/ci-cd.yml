name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  IMAGE_NAME: ai4ap-web

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build Docker image
        run: docker build -t ${{ env.IMAGE_NAME }}:${{ github.sha }} .

      - name: Tag as latest
        if: github.ref == 'refs/heads/main'
        run: docker tag ${{ env.IMAGE_NAME }}:${{ github.sha }} ${{ env.IMAGE_NAME }}:latest

  # Uncomment and configure when ready to deploy:
  #
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Login to Container Registry
  #       run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login <your-registry> -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin
  #
  #     - name: Push image
  #       run: |
  #         docker build -t <your-registry>/${{ env.IMAGE_NAME }}:${{ github.sha }} .
  #         docker push <your-registry>/${{ env.IMAGE_NAME }}:${{ github.sha }}
  #
  #     - name: Deploy
  #       run: |
  #         # Add your deployment command here (SSH, kubectl, az webapp, etc.)
  #         echo "Deploying..."
